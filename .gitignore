node_modules/
*.png
song.txt
survey.txt
logs/
.cache/

# Secretos — NUNCA commitear
google-credentials.json
oauth-credentials.json
token.json
.env

# Efímeros del pipeline (se pisan en cada corrida)
state.json
verify-report.json
suno-lyrics-cache.json
title.txt
chrome_log.txt
selector-drift-report.md
download-click-diagnosis-report.md
pending-gallery-uploads.jsonl

# Perfil de Chrome creado por accidente (bug de backslashes) — tiene cookies/sesiones
UsershectoAppDataLocalChromeAutomationProfile/

# Salidas locales
mp3/
debug/

# Tooling personal de agentes (skills instaladas a mano, no son parte del pipeline)
.agents/

# Basura de trabajo: pruebas sueltas, patches ya aplicados y prompts one-off.
# Nunca commitear — si un scratch se vuelve útil, renombrarlo y agregarlo en serio.
scratch-*
*.patch
prompt-*.md
experiments/

# Carpeta ajena al pipeline (material de estudio) que vive dentro del repo - nunca commitearla.
POO-II-2026/
