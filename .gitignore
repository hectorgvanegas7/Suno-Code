node_modules/
*.png
song.txt
survey.txt
logs/
.cache/

# Secretos — NUNCA commitear
google-credentials.json
oauth-credentials.json
token.json

# Efímeros del pipeline (se pisan en cada corrida)
state.json
verify-report.json
title.txt
chrome_log.txt

# Perfil de Chrome creado por accidente (bug de backslashes) — tiene cookies/sesiones
UsershectoAppDataLocalChromeAutomationProfile/

# Salidas locales
mp3/
debug/

# Basura de trabajo: pruebas sueltas, patches ya aplicados y prompts one-off.
# Nunca commitear — si un scratch se vuelve útil, renombrarlo y agregarlo en serio.
scratch-*
*.patch
prompt-*.md
